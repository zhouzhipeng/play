server_port = 3000
log_level = "DEBUG"

[database]
url=":memory:"

[frp_server]
enabled = false
bind_addr = "0.0.0.0:2333"
default_token = "change_this_token"
heartbeat_interval = 30

[frp_server.services.demo_http]
bind_addr = "0.0.0.0:8081"

[ikev2_server]
enabled = false
auto_install_dependencies = true
# On Debian Bookworm, play-server can automatically install the required strongSwan packages.
# Optional overrides if your strongSwan binaries are not on PATH or use different names.
# daemon_bin = "/usr/sbin/charon"
# swanctl_bin = "/usr/sbin/swanctl"
listen_addr = "0.0.0.0"
port = 500
port_nat_t = 4500
local_id = "vpn.example.com"
server_cert = "certs/ikev2/server-cert.pem"
server_key = "certs/ikev2/server-key.pem"
ca_cert = "certs/ikev2/ca-cert.pem"
pool = "10.10.10.0/24"
dns_servers = ["1.1.1.1", "8.8.8.8"]

[ikev2_server.eap_users]
demo = "change_this_password"
