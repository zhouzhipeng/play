fn main() {
    println!(r#"
    Available commands  are:

    cargo pack  (pack os bundles)
    cargo python  (build embed python embed)
    cargo dev_embed  (build a released binary with embed python)
    cargo dev  (build a released binary by default)
    "#)
}