% table = "test"
use std::sync::Arc;

use axum::extract::State;
use axum::response::Html;
use axum::Router;
use axum::routing::get;
use serde_json::json;

use crate::AppState;
use crate::{R, S};

//remember to register 'init' in mod.rs
pub fn init() -> Router<Arc<AppState>> {
    Router::new()
        .route("/{{table}}", get(test))
}

async fn test() -> R<Html<&'static str>> {
   Ok(Html("ok."))
}

